[project]
name = "dbt"
version = "0.1.0"
description = ""
authors = [
    {name = "Marcos Soares",email = "marcosjmsoares@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core (>=1.11.9,<2.0.0)",
    "dbt-postgres (>=1.10.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "dbt", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
